# TASKS

bundler:
  + 1) right path for module when wrap
  + 2) Test version
    + first build dev
    + repeat build dev: not change, change
    + first build prod
    + repeat build prod: not change, change
  + 3) make config for
    + dev
    + prod
  + 4) Add prefix version for url and change structure

loader:
  1) Without version when dev mode
  2) With version when prod mode
  3) loader.done => loader.module_loaded()
  4) loader.done => loader.module_loaded()
  5) make description for method
  6) debug mode